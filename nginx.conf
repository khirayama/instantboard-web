#user  nobody;
worker_processes  1;

events {
    worker_connections  1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;

    sendfile        on;
    tcp_nopush     on;

    keepalive_timeout  65;

    gzip  on;

    upstream www.instantboard.com {
      server 127.0.0.1:3000;
    }

    upstream app.instantboard.com {
      server 127.0.0.1:3000;
    }

    upstream api.instantboard.com {
      server 127.0.0.1:3001;
    }

    server {
        listen       8080;
        server_name  instantboard.com;

        charset UTF-8;

        location / {
          proxy_pass http://www.instantboard.com/;
        }
    }

    server {
        listen       8080;
        server_name  app.instantboard.com;

        charset UTF-8;

        location / {
          proxy_pass http://app.instantboard.com/;
        }
    }

    server {
        listen       8080;
        server_name  api.instantboard.com;

        charset UTF-8;

        location / {
          proxy_pass http://api.instantboard.com/;
        }
    }

    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}


    # HTTPS server
    #
    #server {
    #    listen       443 ssl;
    #    server_name  localhost;

    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_cache    shared:SSL:1m;
    #    ssl_session_timeout  5m;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
    #    ssl_prefer_server_ciphers  on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}
    include servers/*;
}
